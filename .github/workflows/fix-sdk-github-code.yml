name: Fix SDK GitHub Code

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Create missing SDK files
        run: |
          mkdir -p sdk-github/src/commonMain/kotlin/com/punkcrypto/releasehub/sdk/github

          printf '%s\n' \
          'package com.punkcrypto.releasehub.sdk.github' \
          '' \
          'import io.ktor.client.HttpClient' \
          'import io.ktor.client.plugins.contentnegotiation.ContentNegotiation' \
          'import io.ktor.client.plugins.defaultRequest' \
          'import io.ktor.client.request.header' \
          'import io.ktor.http.HttpHeaders' \
          'import io.ktor.serialization.kotlinx.json.json' \
          'import kotlinx.serialization.json.Json' \
          '' \
          'fun createGitHubHttpClient(): HttpClient {' \
          '    return HttpClient {' \
          '        install(ContentNegotiation) {' \
          '            json(Json { ignoreUnknownKeys = true })' \
          '        }' \
          '        defaultRequest {' \
          '            header(HttpHeaders.Accept, "application/vnd.github+json")' \
          '            header("X-GitHub-Api-Version", "2022-11-28")' \
          '            header(HttpHeaders.UserAgent, "ReleaseHubMvp")' \
          '        }' \
          '    }' \
          '}' \
          > sdk-github/src/commonMain/kotlin/com/punkcrypto/releasehub/sdk/github/GitHubHttpClientFactory.kt

          printf '%s\n' \
          'package com.punkcrypto.releasehub.sdk.github' \
          '' \
          'import kotlinx.serialization.SerialName' \
          'import kotlinx.serialization.Serializable' \
          '' \
          '@Serializable' \
          'data class GitHubRepositorySearchResponse(' \
          '    @SerialName("items")' \
          '    val items: List<RepositoryDto> = emptyList()' \
          ')' \
          '' \
          'typealias GitHubReleaseDto = ReleaseDto' \
          > sdk-github/src/commonMain/kotlin/com/punkcrypto/releasehub/sdk/github/GitHubApiResponses.kt

      - name: Commit fix
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add sdk-github/src/commonMain/kotlin/com/punkcrypto/releasehub/sdk/github

          if git diff --cached --quiet; then
            echo "No changes needed."
          else
            git commit -m "Fix missing GitHub SDK code"
            git push
          fi